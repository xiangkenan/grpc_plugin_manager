syntax = "proto3";

service plugins_rpc
{
	rpc ChooseClassMethod(request) returns (response){}
}

/*
	1:class_name 类名字
	2:parameter 参数
*/
message request
{
	string class_name = 1;
	string parameter = 2;
}

/*
	1:result 返回结果
*/

message response
{
	string result = 1;
}
